#!/bin/sh
# pre-commit: ruff check + ty check
set -e

REPO_NAME=$(basename "$PWD")
echo "==> [$REPO_NAME] ruff check"
uv tool run ruff check .

echo "==> [$REPO_NAME] ty check"
uv tool run ty check .

echo "==> pre-commit passed"
